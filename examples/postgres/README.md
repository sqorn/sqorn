# Sqorn Postgres Example

* Install with `npm install`
* Run with `node main.js`

This example shows how to:

* Connect to postgres
* Delete a database
* Create a database
* Create tables
* Populate tables
* Query tables
